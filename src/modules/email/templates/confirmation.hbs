<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Confirmation</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fc;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
            text-align: center;
        }
        .header {
            font-size: 24px;
            font-weight: bold;
            color: #4a90e2;
            margin-bottom: 20px;
        }
        .message {
            font-size: 16px;
            color: #333;
            line-height: 1.6;
            margin-bottom: 30px;
        }
        .btn {
            display: inline-block;
            padding: 15px 30px;
            background-color: #4a90e2;
            color: white;
            text-decoration: none;
            font-size: 16px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #357ab7;
        }
        .footer {
            font-size: 12px;
            color: #777;
            margin-top: 20px;
        }
        .footer a {
            color: #4a90e2;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <p class="header">Hey {{ name }},</p>
        <p class="message">Please click the button below to confirm your email address and complete your registration:</p>
        
        <p>
            <a href="{{ url }}" class="btn">Confirm Your Email</a>
        </p>

        <p class="message">If you did not request this email, you can safely ignore it.</p>

        <div class="footer">
            <p>Thank you for being a part of our community!</p>
            <p>Need help? <a href="mailto:support@example.com">Contact our support team</a></p>
        </div>
    </div>
</body>
</html>
